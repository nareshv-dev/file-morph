export default function PrivacyPage() {
  return <main id="main" className="history-layout"><section className="page-heading"><div className="eyebrow"><span /> FILE PRIVACY</div><h1>Your files stay private.</h1></section><div className="privacy-content"><h2>Account-owned downloads</h2><p>Uploaded and converted files are available only through authenticated routes scoped to your account. Internal storage URLs are not exposed.</p><h2>Retention and deletion</h2><p>Files expire after the configured retention period, 24 hours by default. You can permanently delete an individual conversion, clear your history, or delete your account to remove files sooner.</p><h2>Conversion limitations</h2><p>PDF pages have fixed geometry while Word, HTML, and Markdown reflow. PDF-to-DOCX output is image-based rather than fully editable text. Advanced layout may change; OCR, macros, scripts, password-protected PDFs, and legacy DOC files are unsupported.</p><h2>Local development</h2><p>When MongoDB is not configured, account metadata is held in memory and resets when the server restarts. The local file adapter stores files on the development machine.</p></div></main>
}
